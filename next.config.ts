import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  images: {
    domains: ['images.unsplash.com','imgs.search.brave.com'],
  },
  // Other configuration options can stay as needed
};

export default nextConfig;
